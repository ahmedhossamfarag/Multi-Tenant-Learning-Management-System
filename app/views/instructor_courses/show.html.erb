<h1><%= @course.title %></h1>
<p><%= @course.description %></p>
<br />
<br />
<%= link_to "contents", course_course_contents_path(course_id: @course.id) %>
<br>
<br>
<%= link_to "enrollments", course_enrollments_path(course_id: @course.id) %>
<br>
<br>
