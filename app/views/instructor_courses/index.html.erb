<div class="list-group">
  <% @courses.each do |c| %>
    <a class="list-group-item" href="<%= url_for instructor_course_path(c.id) %>">
      <h4 class="list-group-item-heading"><%= c.title %></h4>
      <p class="list-group-item-text"><%= c.description %></p>
    </a>
  <% end %>
</div>
